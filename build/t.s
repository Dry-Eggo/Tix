; ------ this file was generated by the Tix compiler -----

extern tputs
extern tput_i
section .data
section .text
; ----- main ---- 
extern t_main
t_main:
    push rbp
    mov rbp, rsp
    sub rsp, 4

    mov qword [rbp - 4], -60

    mov rdi, [rbp - 4]
    sub rsp, 16
    call tput_i
    add rsp, 16
    add rsp, 4
    leave
    ret
section .bss
section .rodata
section .note.GNU-stack