; ------ this file was generated by the Tix compiler -----

extern tputs
section .data
gbval0: db 'Hello World', 0
section .text
; ----- main ---- 
extern t_main
t_main:
    push rbp
    mov rbp, rsp
    sub rsp, 8

    lea rbx, gbval0
    mov qword [rbp - 8], rbx

    mov rdi, rbx
    sub rsp, 16
    call tputs
    add rsp, 16
    add rsp, 8
    leave
    ret
section .bss
section .rodata
section .note.GNU-stack